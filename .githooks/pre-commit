#!/bin/sh
# Prevent committing staged executable files by invoking central script
ROOT_DIR=$(git rev-parse --show-toplevel 2>/dev/null || echo ".")
"$ROOT_DIR/scripts/check_no_exec.sh" || exit 1
exit 0
